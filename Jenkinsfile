pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "Building the project..."'
            }
        }

        stage('Tests') {
            steps {
                sh 'echo "Running tests..."'
            }
        }
    }
}
